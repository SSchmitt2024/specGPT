[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn src.pipeline.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
